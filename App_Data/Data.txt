
File: DBConfig.php:
    -> class DBConfig => info needed to connect to database

File: UserDAO.php
    -> Class UserDAO
        -> public function getAllUsers() 
            => returns a list of all users ordered by last name

        -> public function emailExists($email) 
            => checks if a given email address exists in the db
            => returns either true or false

        -> public function addUser($voornaam, $achternaam, $straat, $nummer, $bus, $postcode, $gemeente, $email, $paswoord, $actief, $admin)
            => uses emailExists($email) 
            => adds new user in db

        -> public function getUserByEmail($email)
            => finds user by email, used when user logs in 
            => returns either NULL or user object

        -> public function getUserByEmailAndPassword($email, $paswoord)
            => finds user by email and password
            => returns an object of User

        -> public function deleteUser($uid)
            => deletes user from db using the id

        -> public function activeSwitch($uid, $actief)
            => changes the state of actief using the id and current state of actief
                => if actief = 0 => becomes 1
                => if actief = 1 => becomes 0

        -> public function getUserById($uid)
            => finds user using the id
            => returns an object of Users